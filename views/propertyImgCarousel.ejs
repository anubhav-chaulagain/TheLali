<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Image Carousel</title>
    <link rel="stylesheet" href="/styles/propertyImgCarousel.css" />
  </head>
  <body>
    <div class="carousel">
      <button class="carousel__prev">&#10094;</button>
      <div class="carousel__slides">
        <% property.images.forEach((image, index) => { %>
          <div class="carousel__slide">
              <img src="<%= image %>" alt="Image <%= index %>" />
          </div>
      <% }) %>
      </div>
      <button class="carousel__next">&#10095;</button>
      <div class="carousel__thumbnails">
        <% property.images.forEach((image, index) => { %>
            <img src="<%= image %>" data-index="<%= index %>" alt="Thumbnail <%= index %>" />
      <% }) %>
      </div>
    </div>
    <script src="/scripts/propertyImgCarousel.js"></script>
  </body>
</html>
